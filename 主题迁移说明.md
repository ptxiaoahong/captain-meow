# Hugo Theme Stack 迁移说明

## ✅ 已完成的工作

1. **安装 Stack 主题**
   - 已通过 git submodule 添加到 `themes/hugo-theme-stack`

2. **更新配置文件**
   - 已将 `hugo.toml` 更新为 Stack 主题配置
   - 保留了原有的菜单、评论、分析等配置
   - 适配了中文内容

3. **内容兼容性**
   - 你的内容在 `content/posts/` 目录，已配置为使用 `posts` 作为主部分
   - 文章格式（TOML frontmatter）完全兼容

## 📝 配置说明

### 主要变更

1. **主题名称**：从 `LoveIt-master` 改为 `hugo-theme-stack`
2. **主部分**：使用 `posts`（你的现有目录结构）
3. **评论系统**：配置为使用 Waline（兼容 Valine API）
4. **菜单结构**：保留了原有的分类和标签菜单

### Stack 主题特性

- ✅ 卡片式设计，适合展示摄影作品
- ✅ 响应式布局
- ✅ 图片画廊支持（PhotoSwipe）
- ✅ 暗色/亮色主题切换
- ✅ 中文支持完善

## 🔍 需要检查的事项

1. **评论系统**
   - 当前配置使用 Waline
   - 如果 Valine 不工作，可能需要调整配置
   - 参考：https://stack.jimmycai.com/config/comments

2. **图片展示**
   - Stack 主题支持图片画廊
   - 你的文章中的图片应该能正常显示
   - 如果图片路径有问题，可能需要调整

3. **菜单图标**
   - 社交链接的图标可能需要调整
   - 参考主题文档选择合适的图标

## 🚀 测试步骤

1. **启动 Hugo 服务器**：
   ```bash
   hugo serve
   ```

2. **访问网站**：
   - 打开浏览器访问 `http://localhost:1313`
   - 检查首页、文章列表、单篇文章是否正常显示

3. **检查功能**：
   - ✅ 菜单导航
   - ✅ 文章列表
   - ✅ 图片显示
   - ✅ 评论系统
   - ✅ 搜索功能（如果启用）

## 📚 参考文档

- Stack 主题文档：https://stack.jimmycai.com
- 配置参考：https://stack.jimmycai.com/config
- GitHub：https://github.com/CaiJimmy/hugo-theme-stack

## ⚠️ 注意事项

1. **Decap CMS 配置**：需要更新 `static/admin/config.yml` 中的永久链接格式（如果需要）
2. **图片路径**：确保图片路径正确
3. **自定义样式**：如果需要自定义，可以在 `assets/scss/custom.scss` 中添加

## 🎨 自定义选项

Stack 主题支持通过以下方式自定义：

1. **自定义 CSS**：在 `assets/scss/custom.scss` 中添加样式
2. **自定义字体**：在配置文件中设置
3. **颜色方案**：支持自动、亮色、暗色模式

## 📝 下一步

1. 测试网站功能
2. 根据需要调整配置
3. 自定义样式（可选）
4. 更新 Decap CMS 配置（如果需要）

